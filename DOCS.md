# 详细文档（配置教程 / 参数说明 / 故障排错）

> 本文件为详细文档。入门引导见 [README.md](README.md)。

## 一、配置教程

### 1.1 添加账号

点击主界面「＋ 添加账号」：

- **名称（可选）**：给账号起名，不填则显示服务商名
- **服务商**：下拉选择要接入的平台。已添加过的服务商选项会变灰（每个模型只允许添加一个账号）
- **API Key**：从各服务商官网的「API Key」页面获取并粘贴
- **接口地址（可选）**：留空用默认地址；通用中转站需填你的中转站地址

### 1.2 各服务商配置要点

| 服务商 | API Key 获取 | 备注 |
|---|---|---|
| DeepSeek | platform.deepseek.com → API Keys | 余额走 HTTP；消费/每日用量需「同步官方」 |
| Kimi | platform.moonshot.cn → API Key 管理 | 余额走 HTTP；每日用量浏览器同步 |
| 智谱 GLM | open.bigmodel.cn → API Keys | **无公开余额接口**，必须浏览器登录 + 同步官方 |
| 硅基流动 | cloud.siliconflow.cn → API 密钥 | 余额走 HTTP；每日用量浏览器同步 |
| OpenRouter | openrouter.ai → Keys | 余额走 HTTP；每日用量浏览器同步 |
| 通用中转站 | 中转站后台生成 | 填中转站地址 + Key；余额走 HTTP |

### 1.3 浏览器登录（智谱 / Kimi / 硅基流动 / OpenRouter）

点击「同步官方」时，软件会弹出内置浏览器窗口。**首次使用需在该窗口登录对应平台一次**，登录态会保存在本地浏览器 profile（`~/.model_balance/browser_profile`），之后自动复用。

> 软件同步使用的浏览器与日常浏览器相互独立，互不影响。

### 1.4 数据存储位置

所有本地数据（含加密后的 API Key）存放在用户目录 `~/.model_balance/`：

- `accounts.json`：账号列表（API Key 用 Windows DPAPI 加密，绑定当前系统用户）
- `settings.json`：设置（主账号、预警、自动刷新、排序等）
- `browser_profile/`：浏览器登录态
- `daily_cache.json` / `*_cache.json`：每日用量 / 各平台同步缓存

## 二、参数说明

### 2.1 自动刷新余额

- 开启后按设定间隔（分钟）自动调用 HTTP 接口刷新余额
- 范围 1~120 分钟，可手动输入，回车 / 点击其他区域保存

### 2.2 自动同步官方

- 开启后按设定间隔（分钟）自动执行浏览器同步（获取消费 / 每日用量等官方数据）
- 范围 1~120 分钟；浏览器同步较慢，间隔建议偏大

### 2.3 主账号

- 每日用量、预警设置都跟随主账号
- DeepSeek 按账号名称自动匹配统计，无需手动绑定 API Key

### 2.4 余额预警

- 「预警设置」：主账号余额低于设定值（元）时弹窗提醒；填 0 或留空关闭

### 2.5 排序

- 排序方式：按添加时间（从早→晚）、按模型余额（高→低）、按名称（首字母）
- 选择后保存，重启保持；同步 / 刷新不改变所选排序

## 三、故障排错

### 3.1 余额显示「网络未连接，稍后自动重试」

- 多为开机 / 网络未就绪，或国外平台（如 OpenRouter）连接慢。稍等自动重试，或检查网络能否访问该平台官网。

### 3.2 某个服务商一直「需同步官方」

- 该平台无公开余额接口（如智谱），或尚未浏览器登录。点击「同步官方」，在弹出的浏览器窗口登录一次即可。

### 3.3 每日用量 / 小时分布没有数据

- 按平台能力而定：DeepSeek / OpenRouter 有 Token 与小时分布；Kimi / 智谱 / 硅基流动只有每日金额，无每小时 Token 分布（平台接口限制）。
- 每日用量数据次日更新（部分平台日账单上午更新），当天可能显示为空。

### 3.4 软件统计与官方网页不一致

- 软件为同步快照，官网为实时数据，存在时间差；一切以服务商网页账单为准。详见 README「已知限制」。

### 3.5 打包 / 运行报错

- 确认已安装依赖：`pip install -r requirements.txt`
- 打包：`pyinstaller ModelBalance.spec --noconfirm`
- 运行打包产物时若提示浏览器问题，确认本机安装了 Edge（Windows 自带）或对应 Chromium 内核。

## 四、开发

- 新增服务商：在 `providers.py` 注册（`PROVIDERS`），余额接口实现 `check_xxx`；每日用量浏览器同步放在独立 `xxx_sync.py`，并在 `gui.py` 接入（参考现有 `kimi_sync.py` / `siliconflow_sync.py` 等）。
- 测试：`python run_tests.py`（自动发现 `tests/test_*.py`，每个模块有对应测试文件）。
